<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Title</title>
</head>
<body>
{% if candfans_user %}
<ul>
  <li>{{ candfans_user.user_id }}</li>
  <li>{{ candfans_user.user_code }}</li>
  <li>{{ candfans_user.username }}</li>
  <li>{{ candfans_user.last_synced_at }}</li>
</ul>
<form method="POST" action="{% url 'candfans_user_refresh' user_code %}">
  {% csrf_token %}
  <button type="submit">更新</button>
</form>
{% else %}
  このユーザ({{ user_code }})は存在しません。作成しますか？
  <form method="POST" action="{% url 'candfans_user_refresh' user_code %}">
  {% csrf_token %}
  <button type="submit">作成</button>
  </form>
{% endif %}
</body>
</html>
